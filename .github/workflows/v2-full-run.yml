name: POLIS v2.0.7 paid workflow sealed

on:
  workflow_dispatch:

jobs:
  sealed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy paid workflow is permanently sealed. Final recovery execution uses v2-resume-final.yml."
