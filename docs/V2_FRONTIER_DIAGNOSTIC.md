# POLIS v2 Frontier Diagnostic

The frontier-model subset is pre-specified but analytically separate from the four-model full backbone.

It uses only the 24 pressure-3 Delegation cells and four governance regimes:

- no institution
- constitutional prompt
- recoverable local guard
- recoverable provenance guard

Models:

- Qwen3.7 Max
- Claude Sonnet 5
- GPT-5.5

The subset tests external validity under the strongest goal-policy conflict. It is not pooled into the primary cheap-model regression and is omitted only if the remaining account balance is insufficient after the complete cheap-model backbone.
