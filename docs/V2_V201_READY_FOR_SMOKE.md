# POLIS v2.0.1 ready for model compatibility smoke

The scientific design is unchanged from the previously validated v2 implementation. Only the three technically incompatible frontier endpoints have been replaced and the protocol version incremented.

Next gate: seven successful strict structured-action compatibility requests. Confirmatory collection remains blocked until this gate passes.
