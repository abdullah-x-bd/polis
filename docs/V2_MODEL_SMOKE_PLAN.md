# POLIS v2 Model Smoke Plan

One non-experimental structured-output request will be sent to each of the seven frozen model endpoints before confirmatory collection. Success requires that the endpoint accepts the current OpenRouter adapter's strict action schema and returns a parseable POLIS action. Smoke outputs are never combined with experimental source data.
